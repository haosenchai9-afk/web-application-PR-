# .gitignore 文件（与脚本同目录）
# 1. 环境变量文件（核心敏感文件）
.github_env
# 2. Python虚拟环境
venv/
# 3. Python编译文件
__pycache__/
*.pyc
*.pyo
*.pyd
# 4. 日志文件（若后续添加日志功能）
*.log
# 5. 操作系统临时文件
.DS_Store  # macOS
Thumbs.db  # Windows
